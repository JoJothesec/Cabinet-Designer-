{
  "name": "cabinet-designer-pro",
  "version": "1.0.0",
  "description": "Browser-based 3D cabinet design tool",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "test:all": "npm run test && npm run test:e2e"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "@vitest/coverage-v8": "^2.0.0",
    "jsdom": "^25.0.0",
    "vitest-canvas-mock": "^0.3.0",
    "@playwright/test": "^1.48.0"
  }
}
